# CHANGELOG — 版本叙事归档

> 从 ARCHITECTURE.md 的「对齐版本」行拆出(v7.36.0 文档瘦身:13 个子代理每次
> 开工都要全文读 ARCHITECTURE.md,历史叙事让每次读取多花数千 token)。
> 本文件**不属于代理必读集**;要考古版本细节来这里或 git log。

## 完整历史(原对齐行原文,含 v7.28.0 及更早)

> **对齐版本：`cyrus-os-v7.35.0`**（v7.35.0 = **P3 场景层**——①新 `scripts/crest.js`(ES module,importmap three):**全站唯一 WebGL 场景「君主徽记」**——App 图标紫 C 徽纹的程序化线框浮雕(斜切六边环带+C 弧+菱形饰+9 轨道光点,零贴图),fixed 悬浮 The 90 面板后,~60s 自旋+呼吸浮沉+滚动加转+指针视差,静息 opacity 0.5;`window.crestIgnite()` 仪式点亮(brass→brass-soft,800ms 线性衰减),由 beat.js ritualPulse(满勤)与 rpg.js sysShowModal levelup 分支(升级)各一行守卫调用;继承旧 herocube/lifetree 全部纪律(DPR≤2、visibilitychange+IO 双暂停、contextlost/restored 整体重建、几何一次生成、材质仅点亮期变更);粗指针/≤768px 降级为静态内联 SVG+CSS 呼吸,reduced-motion 全静态+ignite no-op;data-fx 固定集加 `crest`(index.html 预绘+theme.js SOVEREIGN_FX 同步);②`energyflow.js` 三层深度分层(back 6 条暗慢糊向 #241B45 混 72%/mid 3 条/front 2 条亮快利纯 --brass-soft,手机 2/1/1 背景层优先减),合成亮度低于旧版,`#energy-flow` opacity .9→.5 落进 §2.4 配额;生命周期全保留;③修复 v7.34.0 的查询串失误(index.html components.css?v= 与 sw.js 不一致,离线会 miss),components.css/energyflow.js 查询串双端统一 7.35.0。v7.34.0 = **P2 动效编排 + P1 审查修复**——①**系统窗降临**:六 HUD 开合从卷轴(sysScrollOpen 族 keyframes 退役,`.sys-cover/.sys-lock/.sys-ripple/.sys-rod` 覆层 CSS 隐身)换成 glass.js GSAP 三拍编排(拍1 斜切 hairline 描边成形 120ms→拍2 窗体中线 scaleY 展开 240ms expo.out→拍3 内容分块 stagger 点亮;关=反放 1.4×;经 MutationObserver 旁路侦测 `.open` 类切换,零改模块 JS;`.sys-window` 改 clip-path 斜切+实色);②新 `scripts/beat.js`:`beatTap`(三拍勾选微动效 ≤300ms,连点保护)接进六模块 toggle 手势路径(绑 reconcile 持久行),`ritualPulse`(满勤仪式:辉光+典礼金扫光+粒子,800ms 强制归零,全局互斥)由 toggleThe90 满勤瞬间触发(`_the90PerfectSfxDate` 闩锁原样);③animateNumber 加翻牌微动;glassFlicker 调为「终端点亮」(每字 2 闪定亮);④图标接线:导览 7 频道 PNG/inline SVG→`vendor/icons.svg` Lucide sprite `<use>`(APP_SHELL 换血:-6 nav png +icons.svg+beat.js);SAO 导览/panelRise 重调,sessPulse/todayColBreathe 收敛为纯 opacity 功能微动;封面字 Oswald;⑤**P1 审查 10 项修复**:sovereign.js 交易面板 6s 兜底竞态(改等 body.booted+宽限,glassInitFlip 分拣过滤 .sov-* 三件套,自愈观察器随挂随接)、玻璃退役后 `<button>` UA 默认边框回归(fin 九件+applock 补 `.5px transparent` 基线,store/sys-skill/motiv-card 同法修 border-color 状态规则失效)、暗单主题遗留的 `prefers-color-scheme` 分支(--fin-pos/neg、--fit-pos 收敛为亮值恒定;base.css date/time 控件 `color-scheme:dark`;markets.js TradingView 恒 dark)、system.css 庆祝 brassFlow keyframes 就地补回(仪式限定)、glassInitTrails 补 `[data-fx~="trails"]` 门(隐形画布白烧 CPU)。v7.33.0 = **P1 SOVEREIGN 全站铺开(重设计方向书 REDESIGN.md §5 P1)**——CyrusOS 自此**单一身份「君主终端」**:①tokens.css 值全换(变量名冻结:`--brass=#9D7BE6`/`--brass-soft=#B892FF`、背景三级 `#08060F/#120D22/#1A1330`、`color-scheme:dark` 单暗、`--font-sans`=JetBrains Mono+Noto Sans SC、`--font-serif`=Oswald、radius 全 4px、新增 `--dur-ritual:800ms`);②**四主题退役**——styles/theme-{sterile,terminal,monarch}.css、scripts/{herocube,lifetree,ambient}.js 删除,theme.js 缩为遗留垫片(`initTheme` 固定 stamp `data-theme="sovereign"`+`data-fx="glass flow-additive deco"`,`applyTheme` 为 no-op 令 sync pull 到的旧 theme 值静默失效,settings.theme 列保留不删),主题切换按钮删除,index.html 预绘脚本不再读主题;③**面板语言**——新 `scripts/sovereign.js`(`mountPanelFrame`:16px 斜切 SVG hairline 框+ResizeObserver,`el._sovFramePath` 留给 P2 描边动画;交易面板等 glassInitFlip 分拣完成后再挂框),玻璃 blur 全站退役(唯一保留 system.css `.sys-backdrop` 5px/8px),brassFlow/8 段渐变角标/菱形 kicker/body::before 流光底场退役,渐变 hairline 分隔线,组件壳 4px+两段式 brand focus 环,主页六面板加 `SYS.0x // MODULE` mono 微标签,lifetree-chamber 标记摘除(the90.js lt-HUD 写入有 if 守卫自动 no-op);④九模块 css 皮肤对齐(硬编码色→token,~59 条死主题规则删除,典礼金 `#E8C878` 只存在于升级/成就庆祝弹窗);glass.js 十条 DOM 契约(tilt/--rx--ry、stack-card、rv-pre、in-flight、sys-window 族、booted、isolation)逐条核对保全。v7.32.0 = **P0 重设计地基**(全面重设计方向书见 REDESIGN.md,本版=其 §4 前置改造,零视觉/零行为变化):①渲染层迁「骨架+局部更新」——新模块 `scripts/render-core.js`(ensureSkeleton/setText/setHTML/setClass/setAttr/reconcileList 全局原语,加载序在 state.js 之后、功能模块之前),morning/academics/japanese/trading/todos/the90 六个渲染件改为键控行调和(行级 setHTML 按签名重写;编辑态行不被无关更新撕毁),the90 450 格热力图保留既有签名 memo 整建策略(级联+一次性 IO 观察器整体重建最安全);②主题装饰 guard 收敛为 `<html data-fx>` 能力标志——权威映射 theme.js `THEME_FX`(标志:glass=玻璃交互层/cube=herocube/trails=墨迹/flow-additive=能量丝叠加混合/lifetree=生命树+ambient/deco=默认主题装饰),index.html 预绘脚本内嵌副本**须与 theme.js 同步**,CSS `html:not([data-theme="sterile"])` 全部改 `html[data-fx~="deco"]`(特异性相同),glass.js `glassSterile()`→`glassDisabled()`,额外收编 finance-charts.js:179 与 ambient.js:11 两处漏网 guard;③glass.js 五个逐字重复的 HUD 包裹函数合并为 `mountHudShell(viewId,{hudClass,onClose})`(#system-view 母版不动);④全局层叠 z-index 魔数收敛为 tokens.css `--z-*` 梯度(数值冻结,组件内部局部 z 保留字面量);⑤Web 字体自托管——`vendor/fonts/` 6 个可变字体 woff2(Inter/JetBrains Mono/Oswald × latin/latin-ext,共 228KB,font-weight 区间声明)+ 新 `styles/fonts.css`(unicode-range 保留、family 名不变、display:swap),Google Fonts CDN 三行 link 退役,离线字体自此可用。v7.28.0 = **第 4 主题 monarch《我独自升级》暗影君主**。切换循环 cappa→sterile→terminal→**monarch**→cappa(按钮标签「♛ 君主」)。新 `styles/theme-monarch.css`(仿 theme-terminal 结构,`html[data-theme="monarch"]` 重映射 token 为深紫:`--brass:#9D7BE6`/`--brass-soft:#B892FF`、底 `#08060F`、面板 `#1A1330`)。**满配·系统窗**:不抑制 cappa 装饰——能量流(取 `--brass-soft`,非 sterile 即 additive)、`body::before` 环境光晕、glass-trails、herocube、molten 标题渐变全部读 `--brass/--brass-soft` → **自动变紫**(无需改 JS);额外加面板「系统窗」紫光边 `box-shadow` + 紫色激活(the90-cell.on/primary/focus)。接线:index.html 预绘白名单 +monarch + CSS 链接、theme.js `THEMES`+`updateThemeBtn`、sw.js APP_SHELL+CACHE_VERSION。`settings.theme` 自由字符串、无 DB 迁移。v7.27.7 = **日文打卡清单 → 重复任务**。每个练习项可单独设重复频率(每日/工作日/周末/每周/每两周/每月/自定义天数,沿用 Todo 那套),项加 `repeat`/`customDays`/`since`(锚点,JSONB 无需迁移、旧项缺字段=每日)。`jpItemDueOn(it,date)` 判当天是否到期(周/双周按 since 锚点的星期、月按日号、自定义按间隔天数);**打卡判定改为「完成当天到期的项目即可」**(`jpSettle` 用 `jpDueToday()`,未到期项不挡);清单里未到期项灰显「今日无需」、不可勾、不计入;当天**无到期项**(空清单或当天无排程)时 `ci-btn` 变手动打卡开关。顺带修了 v7.27.5 的 `ci-btn` 仍带 inline `pointer-events:none` 导致手动打卡点不动——`rJP` 现按是否可点动态设 `pointerEvents`。新增/编辑表单都加了重复频率下拉。v7.27.6 = 承接 v7.27.5 修复,补一个残余竞态：merge-push 里「TODAY 本机权威」规则无法区分「主动取消今日」与「本机本会话还没 pull 过(陈旧)」——后者会误删云端今日打卡。加 `jpPulled` 守卫:仅在本设备本会话 **pull 过云端后** 才允许 push 删除 TODAY,未 pull 前一律纯 union,陈旧设备永不抹掉今日。真机实测:打卡今日→云端 log `{2026-04-28, 2026-06-21}`(历史保留+今日新增)、streak 1。v7.27.5 = **日文连续打卡记录再次消失 · 根因修复 + 打卡可用性**。根因(数据丢失)：`japanese` 表把打卡 `log`(JSONB,按日期键)用 **replace-all** upsert/pull,任何 device 只要本地 log 较瘦(常见:某台缓存停在旧值的设备 push)就把云端整段历史覆盖掉 → 别的设备 pull 到被截断的 log → streak(由 log 派生)归零。线上实测 local+cloud 都只剩 `{2026-04-28}`。修复：log 改 **union 合并(永不缩小)**——`jpUnionLog` 在 pull(`sync.js` pullJapanese)与 push(`syncPushJP` 先读云端 log 再并集)两端都合并,本机仅对 **TODAY** 有权威(当天勾/取消);历史天数只增不减,任何陈旧设备都无法再抹掉。**第二因(无法新打卡)**：自动打卡要求「清单全勾」,而用户清单为空(retire 预设后没加项)→ 永远 `all=false` → 打不了卡。修复：`ci-btn` 在清单为空时变回**手动「今日打卡」开关**(`jpCheckInToday` 直接 toggle `log[TODAY]`);`jpSettle` 仅在「清单非空且未全勾」时才自动取消,空清单不再抹掉手动打卡。历史已丢日期无法从任何可访问副本恢复(只剩 4/28)。v7.27.4 = **Todo bug 修复**:勾「过期不顺延(noCarry)」会把**重复任务**也归档隐藏、并断掉循环(变单次)。根因——`sweepExpiredTodos`(todos.js)对任何 noCarry 过期未完成任务一律归档,未豁免重复任务;而下一次循环实例只在「完成时」(toggleTd)生成,故未完成的重复任务被归档后系列即终结。修复:**重复任务(repeat≠none)豁免归档**——repeat 本身就是顺延机制;只「单次」noCarry 过期才归档。并加**自愈**:`sweepExpiredTodos` 唯一会设 archived=true 的地方,故凡 `archived&&重复&&noCarry` 必是旧 bug 误归档→自动恢复(init 本地 + pull 后权威两处 sweep,沿用既有不抢跑 pull 的同步模式)。v7.27.3 = 能量丝带**曲线平滑**(中点二次贝塞尔 `quadraticCurveTo`,消除手机端低采样折线尖角;手机采样 26→40、桌面 46→56;真机 A/B 验证)。v7.27.2 = 能量丝带**调细**(用户反映线条太粗、盖住 Saturday/日期/时钟标题)：`energyflow.js` 三层描边 核心 `×1.3→×0.85`、中层 `×3→×1.9`、外辉光 `×7→×4.2`,基宽 `rnd(1.2,2.8)×1.25→rnd(0.55,1.25)×1.15`,alpha 略降——丝带成细发光线、不再盖标题;`?v=` 同步 `7.27.1→7.27.2`(改了 energyflow.js 必须 bump 查询串否则旧版缓存命中)。真机 Chrome 验证标题清晰可读。v7.27.1 = 能量背景 **URL 级缓存爆破**:`index.html` 对 `components.css`/`energyflow.js` 引用加 `?v=7.27.1`、`sw.js` APP_SHELL 同步+`CACHE_VERSION→v7.27.1`。即使某设备 SW 卡旧版(cache-first),新查询串在旧缓存必 miss→走网络取最新→一刷新即拿到带 isolation 修复的 CSS;`index.html` 本就 network-first 故引用恒为新版;查询串只改 URL 不改路径、服务器忽略,零破坏。真机 Chrome 验证:已加载 `components.css?v=7.27.1`+`isolation:isolate`+canvas litPct~23%。v7.27.0 = 流动能量背景**真正可见**(用户反映 v7.26 仍全黑、经连真机 Chrome 扩展截图三主题定位)：根因——terminal/sterile 的 `body` 背景**不透明**(`theme-*.css` 里 `body{background:#0a0b06/#FAFAFA}`),把 `z:-1` 的 `#energy-flow` canvas 画在 body 底色**之下**、完全遮住;canvas 其实一直在画(litPct~20%),之前所有「读 canvas 像素 toDataURL」验证都因只读缓冲区而**误判成功**。修复=`body{ isolation:isolate }` 给 body 建独立层叠上下文,令 body 自身背景画在它的**负 z 子元素之下**→三主题(cappa 黄铜/terminal 黄/sterile 黄)能量丝带全部显现(cappa 早已是上下文故先前仅它可见;isolation 不为 fixed 子元素建包含块,弹窗/HUD 层级不受影响)。v7.26.0 = 流动能量背景**调到清晰可见**(用户反映 v7.25 看不到)：`#energy-flow` z:-2→**z:-1**(避开不透明 body 背景遮挡)、不透明度 .6→**.9**(terminal .8/sterile .5)、丝带 alpha 上调；`init` **立即先画一帧静态**(不等首个 rAF,即使 rAF 被 visibility 暂停也有内容、首屏即可见)。v7.25.0 = **主页流动能量背景**（`scripts/energyflow.js`，参考《Solo Leveling》系统窗口氛围）：全屏 canvas `#energy-flow`（**z:-2**，比 cube/trails/blob 更深一层）画缓慢漂移的发光能量丝带；**三主题都显示**（不同于 cube/trails 在 sterile/terminal 隐藏），颜色取自当前主题 `--brass-soft`（cappa `#D9BE92`/sterile `#FBFB45`/terminal `#EFE000`），`MutationObserver` 监听 `data-theme` 自动重新着色；深色主题用 `'lighter'` additive 叠辉光、浅色 sterile 用普通混合避免过曝；每主题不同 `opacity`（cappa .6/terminal .5/sterile .32）；生命周期照搬 glass-trails（debounce resize + `visibilitychange` RAF 开关）；`prefers-reduced-motion` 只画一帧静态；全屏 HUD/聚焦时 `display:none`。`init` 等 `load` + `resize()` 用 `innerWidth||clientWidth||screen.width||1280` 兜底（脚本内联早于 layout）。v7.24.0 = 5 项 bug 修复：①原则晚 9pm 核查提醒——整天开着的 tab `init` 不重跑,加每分钟重判 21:00 的计时器(`principlesEveningAutoShow` 自带幂等);②手机打卡闪蓝色选取框盖整面板——全局 `*{-webkit-tap-highlight-color:transparent}` + 可点控件 `user-select:none`(输入框/文本域保留可选可编辑);③`8/9` 倒计时跨午夜停在昨天——the90 tagline 改读实时日期(`new Date().toLocaleDateString('sv-SE')`)而非冻结的 `TODAY`;④日文「记录被删」实为周历只显本周→加「累计 N 天」总数(`Object.keys(S.jp.log).length`;数据其实完好,代理已查线上库确认);⑤交易手机晨间打卡电脑端不同步——电脑「翻篇」在 `pullAll` 前就把全未打卡重置 `saveTR()` 推上云、覆盖手机打卡;改成翻篇 `saveLSRaw` 只写本地不推云、让 `pullTrading` 作权威(晨间 `saveMR`/日文 `saveJP` 同隐患一并堵)。v7.23.0 = **心愿单/商店模块 + 导览黄铜图标**。①心愿单(`store.js`/`store.css`,克隆 calendar HUD 外壳)：记录想买的真实东西——图片(**CyrusOS 首次用 Supabase Storage**,私有桶 `wishlist`,每用户 `用户id/` 文件夹权限,上传+签名链接显示)/价格+币种(复用财务 baseCurrency+finMoney)/名称/描述/购买链接/分类/想要程度/想要⇄已买;顶部统计(想要·总价值≈·已买·已花);新表 `wishlist`(RLS×4 用 `(select auth.uid())` + realtime + index)。**导览加第 7 格 SHOP/心愿**(占位矢量购物袋,待换黄铜版),nth-child 级联动画扩到 7。②导览 6 个图标换成用户自制**黄铜浮雕图标**(从一张拉丝金属板抠出、悬浮无方框):PowerShell+System.Drawing **「亮度保护」flood-fill** 切图(从 4 角算底板亮度,绝不流进明显更亮的像素→保住细的剑刃/火焰细节)+ 300px 高分辨率处理;6 图=芯片/剑/齿轮+循环箭头/叠币+$/日历/篝火,存 `icons/nav/*.png`(img 替原 inline SVG)。v7.18.0 = 导览重做 + 第 3 主题：**terminal 终端主题**（明日方舟式深色战术——`theme-terminal.css`，`html[data-theme="terminal"]` 暗色 token 重映射、黄警示色 `--brass:#C9C90E`、直角斜切面板、mono 标签；herocube/glass-trails 在此主题停渲染；theme.js `THEMES=['cappa','sterile','terminal']` 三态循环）；**SAO 导览抽屉**——原 Markets 抽屉(`drawer.js`)改造成全屏居中导览：左缘单箭头把手 / 右缘左滑 / ← 键开启；6 块斜切板(系统/健身/AI/财务/日历/动力)在磨砂模糊页面上 1→6 级联滑入(SAO 菜单音)；点选播 SAO 选定音→其余 5 块依序淡出、选中块滑向荧幕正中→其 HUD 从该处自带展开；关闭播同一 SAO 音、级联反序退出(PWR 先→SYS 后)；桌面键盘(←开 / ↑↓选 / Enter 定 / Esc 关)；新音效 `sounds/sao-menu*.mp3`。**Markets 退役**(→ 日后并入 Trading Desk 专注页)。v7.17.0 = 全量审计修复批（30-agent 审计 116 项确认 → 实现约 92 项）：**数据完整性**——财务 5 张配置表补 `dirty` 标记+focus 重放（离线编辑不再被 stale 云端覆盖）、跨午夜 `tick()` 自 reload（不再写昨天行）、rehydrate 先 flush 再 reload、pullAll 失败清 `pullAllPromise` 可重试、`pullThe90Daily` 合并保留本地脏 TODAY、pull 守卫(jp/trading/settings 空列表不覆盖)、`cleanJapanese` 进 pull 自愈、todo 拖序只重排可见项、循环交易拉回 cron 的 next_date/last_run。**安全**——应用锁 `?pinreset=1` 须有本次导航的 magic-link token（堵掉裸参数绕过）、锁定退避持久化、CSV 公式注入中和、AI 链接 `javascript:` 方案白名单、搜索在隐私遮罩下不匹配金额。**渲染纯度**——rMR/rFitToday 去掉 DB 写副作用、the90 庆祝/AI 趋势 peek 不再误触发。**财务**——`finFromBase` 修净值/目标换汇(漏 base-rate)、循环跨币种 toAmount 走实时汇率(非 1:1)。**离线/PWA**——SW 导航回退、弹性预缓存(单资产失败不再整体 brick)、SWR 兜底 Response。**性能/杂项**——the90 热力图 450 格签名 memo、herocube 切 sterile 停渲染+WebGL 上下文丢失恢复、lifetree 静止跳算、健身全 +08:00、Lenis 补 fitness-view、reduced-motion 补漏、sterile 配色作用域、焦点轮廓/触控尺寸/aria 一批 a11y、creed.js 死文件删除。**延后(需部署/大改)**：RPG↔rpg-stats.py 镜像那几项(挑战推荐/权重/bool/注释，要 EC2 重部署)、DB RLS 性能迁移(`auth.uid()`→`select`)、字体自托管、replaceTable 跨设备删除、a11y 焦点 trap。v7.16.0 = RPG v2 Phase 3：成就那一波——新增 **自动化 · AUTOMATION** 类(ai_first/vol10/vol50/vol200/streak7/streak30/ship10) + 创造属性成就 attr_cre35 共 8 个；**ACH_TIER 47→55**(rpg.js 目录 + rpg-stats.py 断言同步 + EC2)；attr_balanced 文案修六维；新成就配进度条。v7.15.0 = RPG v2 Phase 2：第 6 属性 **创造/CRE**（雷达五边→六边、战力=6 和，rpgRadarSVG/rpgGrowthSVG 本就泛型）+ 属性接真实活动：**AI 产出 / 健身打卡 自动勾对应 The 90 柱**（`the90AutoMet`，幂等只增不撤；III=AI→智力+创造，IV=健身→力量/敏捷）→ 经验/属性反映真实模块投入；EXP 公式不变（单调）；镜像 rpg-stats.py ATTR_ORDER/ATTR_NAME/ACTIVITY_ATTR +CRE（已 EC2 重部署）；AI 开场动效放慢到 2600ms。v7.14.2 = AI HUD 卷轴开场 AI logo 动效（.ai-cover：黄铜六边环旋转+神经节点+「AI」+副标「AUTOMATION」信号灯式逐字闪入，1500ms 后淡出露内容；仅 cappa，sterile/reduced-motion 跳过；纯 CSS）；v7.14.0 = RPG v2 Phase 1：AI Automation 产出日志 HUD（新表 ai_outputs，独立模块=信号源；记录 构建/学会/交付，派生 streak/活跃天/总数；**本期不接经验/属性/成就**，留 Phase 2/3）；v7.13.2 = The 90 柱 III 课业→AI Automation（暑假重心；信条五柱句 + 服务端战报 rpg-stats.py TARGET_LABEL 同步 + cleanThe90Targets 数据自愈迁移；III→INT 映射不变）；v7.13.1 = HUD tab 真·跟手轮播（拖动时相邻 tab 从边缘实时露出，固定定位幽灵层+id剥离）；v7.13.0 = 专属日历 HUD（新表 cal_events，聚合现有带日期项 + 可增删行程）+ Todo 过期不顺延自动归档（todos +no_carry/archived/archived_at）+ 手机滑动切换（主页三栏 scroll-snap + 各 HUD tab follow-finger pager，新 swipe.js）+ 交易盘前封条（trading +sealed/sealed_at/broke，偏向自动勾 t6）。v7.12.0 = 多代理审计后 35 项 bug/UX 修复：交易日翻篇保留、隐私日合计遮罩、标签 onclick 转义、晨间详情守卫、低谷渡保连续、reduced-motion HUD 外壳、安全区内距、移动端断点、SDK/Chart/three 本地化离线、各 overlay Esc、抽屉 inert 等。SDK 现已 vendored 到 vendor/。）（= sw.js `CACHE_VERSION`，每次 bump 顺手更新此行——

## v7.40.0 — 减法:五件套核心(2026-08-25)

**依据**:对 Supabase 逐表查「总行数 / 最近 30 天 / 最近一次人为写入」,结论——真实存活的只有每日打卡类与轻量待办;
健身(1 次训练记录)/课业(1 条)/日语(3 天)/心愿单(0 条)/推送订阅(0 台设备)等是「为还没发生的行为提前建的容器」,
只贡献精神负担。审查记录见第二大脑 `wiki/notes/cyrus-os-subtraction-review.md`。

**① 保留五件套**:打卡(morning + the90 赛季记分板 + rpg 系统窗 + lowday 低谷日)、待办 todos(+categories)、
日历 calendar、信条与原则 principles(晨读+晚查全保留,Cyrus 钦定)、账本 finance(+charts)。交易台冷冻:代码与面板保留,
沿用 v7.39.0 默认收起;重入条件(2 万美金本金)达成再解冻。

**② 下架并删除**:fitness / ai / store / motivation / hermes / academics / japanese / notifications 八个模块
(scripts + styles + index.html 面板与 HUD + 导览频道 + sw.js APP_SHELL/push 处理),markets.js 死文件一并删除,
cloudflare/ worker 代码删除(线上实例待 dashboard 撤销,见 ARCHITECTURE §7.1)。导览抽屉 7 频道 → 3(系统/财务/日历)。
sync.js 相应 pull/push/realtime/dirty-replay 全部摘除;app.js renderAll/init/rMetrics 同步收缩。
**state.js 一行未动**(S 形态保留,rpg/calendar 的守卫引用安全);**Supabase 无任何 DDL,13 张表冻结保历史**(§3 ❄)。

**③ RPG 红线核查**:`rpgTotalExp` 只读 the90 历史+已存成就(`rpgAchExp` 只读已存键),jp/ac 只进 `rpgTodayExp`
显示用日清计量与守卫式成就测试——删除数据源不会让等级/EXP 回退。fit/ai 成就走 `typeof` 守卫,自然休眠。

**④ 数据侧(非本 repo,同日执行)**:fin_accounts.interest_rate 全部归零 → pg_cron `fin_accrue_interest` 变 no-op,
「利息机器人」停写(2026-06-14 起共 74 笔,历史保留);EC2/Hermes 确认停摆,早间简报(云端定时任务)接棒。

## v7.39.0 — S2 换季 · 目标面板 · 奶白主题 · 面板归档 · 重复待办快进

**① The 90 → 赛季记分板「S2 · 营收之季」**(2026-08-13 → 11-13,93 天,含首尾)。存储与同步一字未改(仍是 `the90_meta`/`the90_daily`,同样的列),换的是窗口、柱子和文案。全部可见文字收进 the90.js 头部一个 **RENAME POINT 常量块**——下次换季只改那一块。四柱 `ai/jp/fit/ball`(AI 生意/日本語/健身/篮球),每根自带 twoMin/badDay/硬标准。**打卡沿用 v7.38.0 的分级评分**(点一下升一级 1→2→3、第四下清空;`the90Num` 兼容旧布尔),但 `the90Graded()` 改为**恒真**——否则新赛季从第 1 天起会落回 standardize 的布尔打卡,正是要避免的倒退;实测证明恒真对全部历史值与旧的分阶段判定逐位等价。三阶段(开局/加速/收成,31 天一段)降级为纯标签+里程碑,`meta.currentPhase` 仍照写。新增**三个销售行动计数器**(陌生开发/跟进/新扫码,± 步进、下限 0),存在**同一个 scores 对象**里——全库无一处遍历 scores(红线:禁 for-in),数字键与柱子键、`_amp` 等命名空间键互不干扰。旧赛季 meta 由 `ensureThe90Defaults` 按起点日期迁移,且**每次渲染重跑**(pull/realtime 会把旧行灌回来),幂等自熄。热力图/本周/打卡格的列数改由 CSS 变量 `--the90-n`/`--the90-days` 驱动。`pullThe90Daily` 窗口 95→110 天(须 ≥ 赛季长度)。旧日行一行不删。

**② RPG 换季保护**(任务书未列,不做即回归):柱子 id 换套后,`rpgMetOn` 若一律按新 id 去数历史日行,全部历史会被算成 0 达标 → 累计 EXP 崩塌 → 等级倒退,直接违反 §5「等级/EXP 永不回退」。故 `rpgTargetIdsFor(date)` 按日期分流新旧 id,满勤 +25 也按「那天当时有几根柱子」判。`RPG_ACTIVITY_ATTR` **整组替换**为四柱(不能留旧行:`rpgAttrFromCounts` 把每行都算进权重和,死行永久拉低属性)——代价是属性从 10 起在赛季前 30 天内爬升。`ai.js`/`fitness.js` 的自动勾柱从 `III`/`IV` 改到 `ai`/`fit`。**rpg-stats.py 尚未同步,见 ARCHITECTURE §7.6。**

**③ 目标面板 `scripts/goals.js`**:标题/说明/期限 + 倒数标签(Xd / 今天 / 逾期 Xd / 达成)、增删改、拖拽排序、达成沉底,位置紧跟赛季面板。**只存 localStorage**(没有 goals 表,本次不动 schema)。首跑种两条:「8/31 前 NT$26,000 宿舍费有着落」「S2 内第一笔生意收入」。

**④ 第三主题「奶白 · Notebook」** `styles/theme-notebook.css`,循环变成 君主 → 终端 → 奶白。照搬 terminal 的做法(作用域 overlay + 最后加载),另处理浅色专有的三处:`color-scheme` 改回 light、深色硬编码色(开屏封面/聚焦遮罩/日期控件 invert 滤镜)逐个点名、重装饰用 **data-fx 只给 `glass`** 一次性关掉(徽记/加色能量丝/紫色氛围层)。金色拆两档:`--brass-soft:#D4A574` 做填充、`--brass:#8F6222` 做小字与线条(纯 #D4A574 当文字只有 1.9:1)。四支荧光笔各带 `-ink`(压暗给文字)与 `-soft`(淡洗给底纹)。页内实测 35 处取样全部达标,最低 4.95。

**⑤ 可归档面板**:`data-panel` 通用机制(app.js `rPanels`),交易台首跑默认收起、一键展开。状态只存 localStorage(不进 SETTINGS_KEYS、不跨设备)。面板 DOM 不删——`tick()` 每秒仍在往 `#sessions` 写。

**⑥ 重复待办快进修复**:`computeNextValidRepeatDate` 一直推到严格晚于今天为止(1000 次安全阀、null-safe)。此前一个 12 天前的每日任务勾一次只前进一天,永远追不上今天。

## v7.36.0 — P4 七 HUD 精修(省 token 配置试点:2×Sonnet + 1×Opus)

七 HUD mono 微标签体系(SYS.00 母舰/07 CAL/08 SHOP/09 PWR/10 FIT/11 AUT/12 FIN,带活统计);字形图标(←/×/✓)全面换 Lucide sprite(三态字形 ✓/−/☐ 与 11px 微钮按可读性保留);克隆 HUD 补 Sfx.open/close(motivation 双层关闭去重;finance 音效天然在低谷锁后);RPG 六维+成就 tier 重校为紫锚 50° 等距色环(全 AA,JS/CSS 镜像同步);死 sysScrollOpen 引用四文件清零。总监督裁定项:旧 the90 celebrate 退役(满勤统一 ritualPulse,由 render 闩锁触发以覆盖 auto-met/realtime 路径,互斥闩吸收手势路径重复)、#dateline 改 ink、markets.js 下线(留档不加载)、herocube 残留 CSS/pillFlash 死值清理、#motiv-player-close 换 sprite。审查修复:图标 color:inherit 精确点名(宿主按钮驱动色)、closeMotivation 补 open 守卫(登出不响幽灵音)、.fin-micro 对齐 .sov-micro 度量、cal/store 死 reduced-motion 块删除。文档拆分:对齐行 12.7k→209 字符挪入本文件,新 AGENT-BRIEF.md(60 行)取代代理三文档全读。遗留低优清理(streak 双算、ai/fit 微标签 innerHTML 模式统一、色板字面量 token 化、markets 孤儿 CSS)记录在案未处理。
